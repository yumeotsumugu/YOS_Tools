# Normalize line endings to LF in the repository
* text=auto eol=lf

# Vendored minified libraries: store verbatim, never convert
PDF_Craft/lib/** -text
Barcode_Generator/lib/** -text
Barcode_Scan/lib/** -text

# Binary assets
*.png binary
*.jpg binary
*.ico binary
