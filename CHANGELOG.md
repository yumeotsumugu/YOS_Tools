# YOS Tools 変更履歴

YOS Tools 全体の更新履歴。新しいものを上にして日付順（`YYYY-MM-DD`、コミット日）で
追記する。各行の先頭に対象（`[共通]` `[トップ]` またはツール名）を付ける。

利用者に見える機能変更を行ったら、このファイルとトップページ
[`index.html`](index.html) の「更新履歴」（`#changelog`）の両方に日本語で追記する。

---

## 2026-09-09

- **[Image Converter]** 変換パターンを拡張。
  - HEIC・WebP・JPEG・JPG・PNG を相互に変換（`canvas.toBlob` による **WebP 書き出し**に対応。非対応ブラウザではエラー表示）。JPEG ⇄ JPG は従来どおり再エンコードなし、PNG→PNG・WebP→WebP も中身はそのまま拡張子だけ変更。
  - **PDF → 画像**：PDF を読み込むとページごとに一覧へ展開し、各ページを JPG／JPEG／PNG／WebP で書き出す。解像度スライダー（1〜3倍、既定は等倍）付き。同梱ライブラリは pdf.js（`lib/pdf.min.js` ＋ `lib/pdf.worker.min.js`、PDF Craft と同じもの）。
  - **画像 → PDF**：画像を 1 枚ずつ個別の PDF に（既定）、または「1つの PDF にまとめる」を選択可能。ページサイズは画像の実寸。同梱ライブラリは pdf-lib（`lib/pdf-lib.min.js`）。
  - HEIC への書き出しは、エンコーダが大容量かつ GPL 系ライセンスのためブラウザ単体では非対応（読み込みは従来どおり可能）。UI にもその旨を明記。
  - 出力形式に応じて画質・背景色・EXIF・PDF解像度・まとめ方の各項目を出し分け。設定は `yos.imageconv.opts.v1` に保存。
- **[トップ]** ツール一覧に検索ボックスを追加。ツール名・説明・タグに加え、各ツールへ用意した検索用キーワード（別名・英語表記・略称・「〜したい」の言い回しなど）に対しても絞り込む。スペース区切りは全語 AND 一致、カタカナ⇄ひらがな・英字の大文字小文字の違いは吸収。`Esc` またはクリアボタン（×）でリセット、一致なしのときはその旨を表示、検索中は「準備中」カードを隠す。

## 2026-09-07

- **[Barcode Generator]** ツールを追加。値と規格（CODE128／128-A/B/C、JAN・EAN-13、EAN-8、UPC-A、CODE39、ITF、ITF-14、MSI、Pharmacode、Codabar）を選んで 1 次元バーコードを生成。バーの幅（**整数ピクセルのみ**＝半端な幅だとバーが滲んで読み取れなくなるため）・高さ・余白、値の文字表示（サイズ／位置／寄せ）、線と背景の色（透明可）を調整でき、SVG プレビュー、PNG（3 倍で書き出し）／SVG 保存、画像コピー、生成履歴の端末内保存に対応。規格ごとの入力条件をヒント表示し、読み取れない値は無効として明示（MSI・Pharmacode は一般的なスキャナーで読めない旨も表示）。同梱ライブラリは JsBarcode。
- **[Barcode Scan]** ツールを追加。カメラ、またはバーコードが写った画像から中身の番号・テキスト・URL を読み取る。CODE128／39／93・JAN／EAN・UPC・ITF・Codabar などの 1 次元コードのほか QR・Data Matrix・PDF417・Aztec にも対応（MSI・Pharmacode は読み取り不可）。対応ブラウザでは標準の `BarcodeDetector` をタイムアウト付きで先行使用し、常に同梱の ZXing でも読み取り。ZXing 側は複数のバイナライザ・Otsu 2値化・拡大率・明暗反転・中央帯の切り出し・90°回転を組み合わせ、Codabar／Code93 も明示指定で拾う。検出できなかったときは理由（ライブラリ未読込・画素読み取り不可・解析サイズなど）を表示。カメラは**連続で自動読み取り**し（同一コードの連打は抑制）、読み取った結果を1つの一覧に順番にためて端末内に保存、行ごとの削除・全消去・**CSV 書き出し**（BOM 付き UTF-8、列＝番号／内容／規格／種類／読み取り日時）に対応。規格の判定、種類（URL・Wi-Fi・商品コードなど）の判定、URL のその場リンク表示にも対応（従来の「履歴」は結果一覧に統合）。
- **[PDF Craft]** 「画像→PDF」「PDF→画像」を追加。画像（JPEG・PNG・WebP など）を読み込んで並べ替え・回転してから 1 つの PDF にまとめる（用紙は画像サイズのまま／A4・レターにフィット）。逆に PDF の各ページを PNG／JPEG 画像として書き出す（解像度 1〜3 倍、複数ページは ZIP、選択ページのみも可）。PDF・画像の混在も可。
- **[Wareki]** 和暦西暦 早見表ツールを追加。西暦と和暦（明治〜令和）の相互変換（改元をまたぐ年は「昭和64年／平成元年」のように両方表示、元号の境界日で正確に判定）、生年月日からの満年齢・数え年・干支、西暦範囲を指定して作る早見表（印刷・CSV コピー対応）。
- **[Calc]** 関数電卓ツールを追加。三角関数・対数・累乗・平方根・階乗などに対応し、式をそのまま入力して計算できる（`eval` は使わず自前の式パーサで評価）。度／ラジアン切替、メモリ（MC/MR/M+/M−）、直前の答え `Ans`、暗黙の掛け算（`2π` など）、キーボード入力、計算履歴の端末内保存に対応。
- **[QR Scan]** ツールを追加。カメラ、または QR コードが写った画像から中身のテキスト・URL を読み取る。対応ブラウザでは標準の `BarcodeDetector` を使い、非対応時は同梱の jsQR で読み取り。URL はその場で開けるリンクとして表示、種類（Wi-Fi・メール・電話・vCard など）の判定、読み取り履歴の端末内保存に対応。
- **[Text Cleaner]** 「PDF・段落」オプションを追加。PDF からコピーしたときの 1 行ごとの強制改行を段落へつなぎ直す（空行は段落の区切りとして残し、箇条書き・番号見出しの行は結合しない）。行末で分割された英単語のハイフン結合、句点（`。！？`）ごとの改行にも対応。「PDF貼り付け例」ボタンを追加。
- **[QR Generator]** QR コードの中心にロゴ画像を重ねる機能を追加（大きさ％・白い下地の調整に対応、PNG / SVG どちらの書き出しにも反映）。
- **[QR Generator]** 前景色・背景色の入力欄で、色見本の外側をクリックしてもカラーパレットが開く不具合を修正。
- **[QR Generator]** 前景色・背景色を 16 進コード（`#rrggbb` ／ `#` 省略・3 桁短縮）で直接入力できるようにした。
- **[カラーパレットマスター]** 履歴の記録タイミングを調整。カラーピッカーのドラッグ中は中間色を記録せず、操作が落ち着いた時点（またはピッカーを閉じた時点）の色だけを履歴へ残すようにした。履歴・ブックマーク・ハーモニー・グラデーション・パレットからの色選択はすぐ記録。
- **[共通]** 変更履歴を YOS Tools 全体で一元管理する方式に移行（正本 `CHANGELOG.md` ＋ トップページ `#changelog`）。ページ内の個別変更履歴 UI は廃止。
- **[Image Converter]** ツールを追加。HEIC・WebP・JPEG・PNG などを JPG / JPEG / PNG へ一括変換（JPEG ⇄ JPG は再エンコードせず拡張子のみ変更）、簡易 EXIF 操作、ZIP 一括保存に対応。
- **[トップ]** 「YOS Toolsとは」セクションを追加。
- **[共通]** 各ツールとトップページのヘッダーをスクロール追従（固定）に変更。
- **[トップ]** ページ最大幅を広げ、思想・ABOUT・利用規約の幅と改行を調整。

## 2026-09-06

- **[共通]** YOS Tools として発足。トップページと PDF Craft / カラーパレットマスター を1つのリポジトリに統合。
- **[ImageMaker]** ツールを追加（画像／テキスト／図形の配置、レイヤー、フィルター、PNG・APNG 書き出し、プロジェクト保存）。
- **[QR Generator]** ツールを追加（URL・テキストから QR 生成、誤り訂正レベル／サイズ／色の調整、PNG・SVG 保存、生成履歴）。
- **[Text Cleaner]** ツールを追加（改行・連続スペース・空行の整理、全角半角変換、重複行削除、行頭記号の一括付与）。
- **[共通]** 各ツールの UI/UX をトップページのデザインシステムに統一（`assets/common.css` にカラートークン・タイポグラフィ・共通パーツを集約）。
- **[共通]** テーマ設定（システム / ライト / ダーク）を全ツール共有化（`assets/theme.js`、`localStorage` キー `yos.theme`、タブ間同期）。
- **[共通]** URL 整形を `theme.js` に一本化し、各ツールの URL からも `index.html` を隠すようにした。
- **[共通]** ファビコンとトップページのロゴマークを共通アイコン（おとり様 @ai_romoti）に変更。
- **[共通]** 開発ガイド `CLAUDE.md` を作成。
- **[トップ]** 作者プロフィール（夢生ツムグ）を追加。
- **[トップ]** 利用規約・ライセンスを `#terms`（折りたたみ）に集約し、内容を改訂。各ツールからはリンクのみ。
- **[トップ]** 羽根と流れ星の装飾アニメーションを追加。
- **[共通]** 各ツールの「← ツール一覧」リンクを `../index.html` に統一（完全ローカルの `file://` でもトップに戻れるように）。
- **[トップ]** プロフィールのリンクを notion.site の URL に修正。

---

## 統合前の履歴（カラーパレットマスター単体版）

カラーパレットマスターは YOS Tools への統合（2026-09-06）より前に、単体ツールとして
公開されていた。以下はそのページ内に記録されていた変更履歴。

### 2026-05-17 — v1.1.0

- ライトモード / ダークモードを追加（システム / ライト / ダークの切り替え、OS 設定の自動検出と追従）。
- ブラウザアイコン（favicon）を追加。

### 2026-02-17 — v1.0.1

- 変更履歴を折りたたみ式に変更（既定は閉じた状態）。
- ヘルプモーダル内のスクロール動作を改善。
- Tailwind CDN 使用についての注記を追加。

### 2026-02-17 — v1.0.0（初回リリース）

- カラーピッカー、HEX / RGB / RGBA / HSL / HSLA 形式でのコピー。
- ブックマーク、履歴（最近使った 12 色）。
- カラーハーモニー生成（補色・類似色・トライアド）、グラデーション生成。
- WCAG コントラストチェッカー、パレット管理。
- JSON エクスポート / インポート、localStorage による自動保存、使い方ガイド。
